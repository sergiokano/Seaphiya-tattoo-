export interface NavItem {
  id: string;
  label: string;
  href: string;
}

export interface TattooWork {
  id: number;
  title: string;
  image: string;
  category: string;
}

export interface MousePosition {
  x: number;
  y: number;
}
